/**
 * Add
 * Subtract
 * Divide
 * Multiply
 */

const add = (a,b) => a + b
const subtract = (a,b) => b - a
const multiple = (a,b) => a * b
const divide = (a,b) => b / a


let n1 = 1
let n2 = 0
let operand = ''

console.log(add(n1,n2))

// function operate(){
//     switch (key) {
//         case value:
            
//             break;
    
//         default:
//             break;
//     }
// }